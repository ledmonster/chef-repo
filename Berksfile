site :opscode
cookbook 'yum'
cookbook 'mysql'
cookbook 'python'
# cookbook 'ssh_known_hosts'  # This cookbook requires chef search functionality, which chef-solo doesn't support
# cookbook 'ssh-util'  # This cookbook doesn't work, because the name doesn't match with LWRP conventions

cookbook 'ssh', github: "markolson/chef-ssh"
